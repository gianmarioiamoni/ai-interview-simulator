You are an expert Python tester.

Given this coding problem:

{{ problem }}

Authoritative Function Contract (use this for all test cases — do NOT derive the function name or parameters from the problem description above):

- Function name (entrypoint): {{ entrypoint }}
- Parameters (in order): {{ parameters }}
- Parameter count: {{ param_count }}

Generate {{ num_tests }} diverse test cases that exercise the function `{{ entrypoint }}`.

Return STRICT JSON:

{% raw %}
[
  {
    "args": [...],
    "expected": ...
  }
]
{% endraw %}

CRITICAL RULES:
- Call the function as: {{ entrypoint }}({{ param_example }})
- args length MUST be exactly {{ param_count }} (one element per parameter)
- args MUST follow the parameter order: {{ parameters }}

Examples of correct args structure:

If parameters = ["nums"], then:
args = [[1,2,3]]

If parameters = ["nums", "target"], then:
args = [[2,7,11,15], 9]

If parameters = ["person"], then:
args = [{"name": "John", "age": 30}]

{{ domain_hints_block }}
Rules:
- args MUST match the parameter order exactly
- expected MUST be correct
- Include edge cases:
  - empty input
  - single element
  - duplicates
  - boundary values
- No explanations
- No markdown
- Only JSON